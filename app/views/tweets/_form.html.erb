<%= form_for(@tweet) do |f| %>
  
  <% if @tweet.errors.any? %>
      <%= @tweet.errors.messages[:content][0] %>
  <% end %>

  <br>
  <p>
    いまどうしてる？<br>
    <%= f.text_field :content %>
  </p>

  <p>
    <%= f.submit 'ツイート' %>
  </p>
<% end %>